package com.utilities.vibal.utilities.util;import android.app.Activity;import android.app.AlertDialog;import android.content.Context;import android.content.DialogInterface;import android.renderscript.ScriptGroup;import android.util.Log;import android.view.View;import android.view.inputmethod.InputMethodManager;import com.utilities.vibal.utilities.R;import com.utilities.vibal.utilities.models.CashBoxManager;import java.io.File;import java.util.Arrays;import androidx.annotation.NonNull;public class Util {    private static final String TAG = "PruebaUtil";    //Creates an alert dialog showing the help of the activity    public static AlertDialog showHelp(Context context,int helpTitle,int helpResourceString) {        AlertDialog.Builder builder = new AlertDialog.Builder(context);        builder.setTitle(helpTitle);        builder.setMessage(helpResourceString);        builder.setPositiveButton(R.string.button_gotIt, null);        AlertDialog dialog = builder.create();        dialog.setCanceledOnTouchOutside(false);        return dialog;    }    //Shows the keyboard    public static void showKeyboard(@NonNull Context context, View view){        InputMethodManager input = (InputMethodManager) context.getSystemService(Context.INPUT_METHOD_SERVICE);        input.showSoftInput(view,InputMethodManager.SHOW_IMPLICIT);    }    /**     * Renames a File     * @param originalFileName name of the original file     * @param newFileName new name of the file     * @param context context in which it is being called     * @return true if and only if the renaming succeeded; false otherwise     *///    public static boolean renameFile(String originalFileName, String newFileName, @NonNull Context context){//        File originalFile = context.getFileStreamPath(originalFileName);//        File newFile = new File(originalFile.getParent(), newFileName);//        if(newFile.exists())//            context.deleteFile(newFile.getName());////        return originalFile.renameTo(newFile);//    }}